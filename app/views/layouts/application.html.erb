<!DOCTYPE html>
<html lang="en">
  <%= render 'layouts/head' %>
<body>

  <%= render 'layouts/nav' %>
    <div class="container">
        <%= yield %>
        <%= render 'layouts/footer' %>
        <%= debug(params) if Rails.env.development? %>
      </div><!-- /.container -->
  </body>
</html>
